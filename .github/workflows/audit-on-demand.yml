name: Run Phase Audit (v7.5)

on:
  workflow_dispatch:
    inputs:
      repo:
        description: 'Target repo (owner/repo)'
        required: true
        default: 'johnnylugm-tech/tts-kokoro-v613'
      phase:
        description: 'Phase number (1-8)'
        required: true
        default: '1'
      branch:
        description: 'Target branch'
        required: false
        default: 'main'

jobs:
  audit:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    env:
      # GitHub token for gh CLI (works for public repos)
      GH_TOKEN: ${{ secrets.GH_PAT || github.token }}

    steps:
      - name: Checkout claude_audit repo
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Verify gh CLI installed
        run: |
          which gh >/dev/null 2>&1 && gh --version || echo "gh not in PATH, using fallback"
          # GitHub Actions runners include gh CLI by default on ubuntu-latest

      - name: Run audit (phase_auditor.py v7.5)
        id: audit
        run: |
          set -e
          # Build proper report directory: reports/OWNER/REPO/BRANCH/
          REPO="${{ github.event.inputs.repo }}"
          OWNER=$(echo "$REPO" | cut -d/ -f1)
          REPO_NAME=$(echo "$REPO" | cut -d/ -f2)
          BRANCH="${{ github.event.inputs.branch }}"
          REPORT_DIR="reports/${OWNER}/${REPO_NAME}/${BRANCH}"
          mkdir -p "$REPORT_DIR"
          REPORT_FILE="${REPORT_DIR}/audit_phase${{ github.event.inputs.phase }}.md"
          python3 phase_auditor.py \
            --repo "$REPO" \
            --phase "${{ github.event.inputs.phase }}" \
            --branch "$BRANCH" \
            --save "${REPORT_FILE}"
          # Export report path for downstream steps
          echo "report_file=${REPORT_FILE}" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Extract verdict from report
        id: verdict
        run: |
          REPORT="${{ steps.audit.outputs.report_file }}"
          if [ -z "$REPORT" ]; then
            echo "❌ Audit failed or report path not exported" >&2
            echo "VERDICT=ERROR" >> $GITHUB_OUTPUT
            echo "REPORT=" >> $GITHUB_OUTPUT
            exit 1
          fi
          if [ ! -f "$REPORT" ]; then
            echo "❌ Report file not found: $REPORT" >&2
            echo "VERDICT=ERROR" >> $GITHUB_OUTPUT
            echo "REPORT=" >> $GITHUB_OUTPUT
            exit 1
          fi
          # Extract verdict from table: "| 裁決 | ✅ **通過** |"
          VERDICT=$(sed -n '/^| 裁決 |/p' "$REPORT" | sed 's/^|[^|]*|\s*//;s/\s*|$//' | sed 's/<[^>]*>//g' | xargs)
          if [ -z "$VERDICT" ]; then
            echo "❌ Failed to extract verdict from report" >&2
            VERDICT="ERROR"
          fi
          echo "VERDICT=$VERDICT" >> $GITHUB_OUTPUT
          echo "REPORT=$REPORT" >> $GITHUB_OUTPUT

      - name: Commit and push report
        if: steps.verdict.outcome == 'success'
        run: |
          set -eo pipefail
          git config user.name "claude-audit-bot"
          git config user.email "audit-bot@noreply.github.com"
          git add reports/ || true

          VERDICT="${{ steps.verdict.outputs.VERDICT }}"

          if git diff --cached --quiet; then
            echo "✅ No new report changes to commit."
          else
            git commit -m "audit: ${{ github.event.inputs.repo }} phase ${{ github.event.inputs.phase }} (${{ github.event.inputs.branch }}) — ${VERDICT}" || {
              echo "⚠️ Commit failed (may be protected branch)"
              exit 0
            }
            if git push; then
              echo "✅ Report committed and pushed."
            else
              echo "❌ Push failed - check branch protection or auth" >&2
              exit 1
            fi
          fi

      - name: Job summary
        if: always()
        run: |
          REPORT_FILE="${{ steps.verdict.outputs.REPORT }}"
          if [ -f "$REPORT_FILE" ]; then
            echo "## Phase ${{ github.event.inputs.phase }} Audit Report" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Repo**: ${{ github.event.inputs.repo }}" >> $GITHUB_STEP_SUMMARY
            echo "**Verdict**: ${{ steps.verdict.outputs.VERDICT }}" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details><summary>Full Report</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            cat "$REPORT_FILE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
          fi
